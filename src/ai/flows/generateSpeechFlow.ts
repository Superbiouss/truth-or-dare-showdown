'use server';

// This Genkit flow has been disabled and replaced with the browser-native
// Web Speech API for text-to-speech. This change was made to avoid hitting
// API quota limits, which was causing the AI Announcer feature to fail.
// The new implementation is more reliable and has no usage costs.
export {};
